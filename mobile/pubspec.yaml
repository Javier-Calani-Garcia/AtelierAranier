name: mobile_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Estado (equivalente a los signals/services de Angular)
  flutter_riverpod: ^2.6.1

  # Cliente HTTP con interceptors (adjunta JWT, maneja 401)
  dio: ^5.7.0

  # Ruteo con guards por redirect (equivalente a authGuard/adminGuard/permisoGuard)
  go_router: ^14.6.2

  # Almacenamiento seguro del token (equivalente a localStorage, pero cifrado)
  flutter_secure_storage: ^9.2.2

  # Tipografia Inter, igual que la web
  google_fonts: ^6.2.1

  # Cache de imagenes de producto (vienen de Supabase Storage)
  cached_network_image: ^3.4.1

  # Google Sign-In nativo
  google_sign_in: ^6.2.2

  # Abre el link de WhatsApp en Cotizaciones
  url_launcher: ^6.3.1

  # Persistencia local del carrito (sin backend de carrito todavia)
  shared_preferences: ^2.3.3

  # Video de fondo del hero, igual que en la web
  video_player: ^2.9.2

  # Seleccionar imagenes de la galeria para subir fotos de producto (CU05)
  image_picker: ^1.1.2

  # CU19: reconocimiento de voz (dictar la pregunta al chatbot) -- equivalente
  # nativo del Web Speech API que usa la web, que no existe en Flutter.
  speech_to_text: ^7.0.0

  # CU19: sintesis de voz (que el bot conteste hablado).
  flutter_tts: ^4.2.0

  # Probador AR en vivo (CU09, motor Decart): @decartai/sdk es un paquete
  # JS sin equivalente Dart, asi que se reutiliza la misma pagina que ya
  # funciona en la web dentro de un WebView, en vez de reimplementar el
  # protocolo WebRTC de Decart a mano en Dart.
  webview_flutter: ^4.10.0
  # WebViewController.onPermissionRequest solo reenvia el pedido de la
  # pagina al WebView; en Android 6+ el permiso de camara del SISTEMA
  # operativo (runtime, no solo el <uses-permission> del manifest) tiene
  # que estar concedido antes, o el grant() de la pagina no sirve de nada.
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Genera el icono de lanzador (y el legacy/Play Store) a partir de
  # assets/icon/*.png — ver seccion `flutter_launcher_icons` mas abajo.
  flutter_launcher_icons: ^0.14.3

  # Genera la splash screen nativa (la que se ve entre tocar el icono y el
  # primer frame de Flutter) con nuestro logo en vez del logo de Flutter.
  flutter_native_splash: ^2.4.3

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon/icon.png"
  adaptive_icon_background: "#203C40"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"

flutter_native_splash:
  color: "#FFFFFF"
  image: assets/logo.png
  android_12:
    # En Android 12+ el ícono central SIEMPRE se recorta a un círculo (regla
    # del sistema operativo, no configurable) -- "splash_icon.png" es el
    # mismo logo pero con mucho mas margen alrededor para que ese recorte
    # no le coma el texto.
    image: assets/splash_icon.png
    color: "#FFFFFF"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/qr-transferencia.png
    - assets/video/hero.mp4
    - assets/video/tienda-hero.mp4

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
